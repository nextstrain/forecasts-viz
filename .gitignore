# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# for dev purposes we may store data locally
/data

# puppeteer generated images etc
/figures

# dependencies
/node_modules

# library build (`npm run transpile`)
/dist

# test app build (`npm run build`)
/build

# don't accidentally commit tarballs from `npm pack`
nextstrain-forecasts-viz-*.tgz

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
